spring:
  application:
    name: ai-service
  data:
    mongodb:
      uri: mongodb+srv://sagar:Sagar%402806@cluster0.ccqiy5k.mongodb.net/fitnessrecommendation?retryWrites=true&w=majority&tls=true
      database: fitnessrecommendation

server:
  port: 8083

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
